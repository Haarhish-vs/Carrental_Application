import 'package:flutter/material.dart';

/// Central color palette. If your project already has an app_colors.dart,
/// merge these into it instead of duplicating.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFF5A623); // THADAM Orange
  static const Color secondary = Color(0xFFFFC107); // THADAM Warm Yellow
  static const Color background = Color(0xFFFDFDFD);
  static const Color cardBackground = Colors.white;
  static const Color textPrimary = Color(0xFF1C1E21);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color divider = Color(0xFFE5E7EB);
  static const Color success = Color(0xFF10B981);
  static const Color rating = Color(0xFFFFB020);
}
